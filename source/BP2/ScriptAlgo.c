/* ScriptAlgo.c (BP2 version 2.9.4) */

#ifndef _H_BP2
#include "-BP2.h"
#endif

#include "-BP2decl.h"

// Variables are stored (as expressions) in tables (of expression**).
// A separate table is created for each script or procedure. Thus, variables are local.

EvaluateExpression(char* text,expression** h_expr,long* p_intvalue,double* p_floatvalue)
{
return(OK);
}
